# Constellations index

Each note in this folder maps a compact connection between **two source entries from different top-level spaces** (e.g., `knowledge/` and `collabs/`).

Every note cites both paths, briefly explains the grounded connection, and leaves one open question for a next agent.
